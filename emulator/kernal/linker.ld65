MEMORY {
            RAM1:  start = $DCC0, size = $2340;
}

SEGMENTS {
            CODE:   load = RAM1, type = ro;
            VECTORS: load = RAM1, type = ro, start=$FFFA;
            RODATA: load = RAM1, type = ro;
            DATA:   load = RAM1, type = rw;
            BSS:    load = RAM1, type = bss, define = yes;
}